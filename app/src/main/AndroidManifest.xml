<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="net.chaosworship.topuslibtest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".benchmark.BenchmarkActivity">
        </activity>
        <activity android:name=".drawingboard.DrawingBoardActivity">
        </activity>
        <activity android:name=".inputtest.InputTestActivity">
        </activity>
        <activity android:name=".bitmapgen.BitmapGenActivity">
        </activity>
        <activity android:name=".inputtest.WidgetActivity">
        </activity>
        <activity android:name=".particles.ParticlesActivity">
        </activity>
    </application>

</manifest>